<?php require VIEWS_PATH . 'admin/includes/header.phtml'; ?>

<div class="container">
    <div class="col-md-12 mt-4">
        <h1>AdminLoja - <small>Todos os Usuários</small>
            <a href="<?= HOME; ?>/admin/users/new" class="btn btn-primary" style="margin-left: 400px;">Criar Novo Usuário </a>
        </h1>
        <hr>
    </div>

    <div class="col-md-12">
        <?php require VIEWS_PATH . 'includes/messages.phtml'; ?>
    </div>

    <div class="col-md-12">
        <table class="table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Nome</th>
                    <th>Criado em</th>
                    <th></th>
                </tr>
            </thead>

            <tbody>
                <?php foreach ($this->users as $user) : ?>

                    <tr>
                        <td><?= $user['id']; ?></td>
                        <td><?= $user['first_name'] . ' ' . $user['last_name']; ?></td>
                        <td><?= date('d/m/Y H:i:s', strtotime($user['created_at'])); ?></td>
                        <td>
                            <a href="<?= HOME ?>/admin/users/edit/<?= $user['id']; ?>" class="btn btn-primary">Editar</a>

                            <a href="<?= HOME ?>/admin/users/remove/<?= $user['id']; ?>" class="btn btn-danger">Excluir</a>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>

<?php require VIEWS_PATH . 'admin/includes/footer.phtml'; ?>